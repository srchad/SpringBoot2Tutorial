server:
  port: 8000
  devtools:
    restart:
      log-condition-evaluation-delta: false
      exclude: static/**,public/**

my:
  secret: ${random.value}
  num: ${random.int}
  bignumber: ${random.long}
  uuid: ${random.uuid}
  number:
    less:
      than:
        ten: ${random.int(10)}
    in:
      range: ${random.int[1024,65536]}

logging:
  config: classpath:logback-dev.xml
  level:
    root: WARN
    com.tistory.lky1001.boot2tutorial: DEBUG
    org.springframework.web: DEBUG
    org.hibernate: ERROR

---

spring:
  profiles: dev

---

spring:
  profiles: prod
